#!/usr/bin/env bash

# this script should run on the first environment setup.
# it does two main things:
#   1. mine some blocks and create dummy transactions so lightning nodes can estimate fees
#   2. run the setup_nodes script, that establishes the lightning channels

fill-mempool
cd $LNPATH/py
./setup_nodes.py
